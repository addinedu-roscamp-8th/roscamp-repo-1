# 음성 주문 GUI (voice_feedback_widget.py) 실행 시 필요
# 사용: pip install -r requirements-voice-gui.txt
#
# 마이크 녹음(sounddevice)은 시스템에 PortAudio 필요:
#   Ubuntu/Debian: sudo apt install libportaudio2
#   Fedora: sudo dnf install portaudio-devel
requests>=2.28.0
sounddevice>=0.4.6
numpy>=1.20.0
