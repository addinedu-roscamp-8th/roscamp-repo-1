# TCP 통신 설정
ORDER_MS_HOST=127.0.0.1
ORDER_MS_PORT=5000

# FMS (Food Management System) 설정
FMS_HOST=127.0.0.1
FMS_PORT=5001

# 로봇 서비스 설정
ROBOT_SERVICE_HOST=127.0.0.1
ROBOT_SERVICE_PORT=5002

# GUI 설정
SCREEN_WIDTH=1024
SCREEN_HEIGHT=768
FULLSCREEN=true

# 테이블 번호 (각 태블릿마다 다르게 설정)
TABLE_NUMBER=1

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
