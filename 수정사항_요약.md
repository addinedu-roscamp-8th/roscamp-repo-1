# 중요 수정사항 - Kitchmatics FMS 디버그 세션

**날짜**: 2026-02-26
**세션**: FMS 다중 로봇 시스템 오케스트레이션 및 디버그

---

## 발견 및 수정된 문제들

### 1. 근본 원인: "A_FAIL:busy" 에러 ✅

**에러 메시지**:
```
[ERROR] [sandwich_coordinator]: A finish failed: A_FAIL:busy
[ERROR] [sandwich_coordinator]: Failed to make sandwich for order ORD-20260226073129-0001
```

**근본 원인**:
- **위치**: `robot_arm/sandwich_arm_ws/src/mycobot_kitchen_nodes/mycobot_kitchen_nodes/recipe_executor_node.py:343`
- **문제점**: Arm A (recipe_executor_node)가 이미 작업을 처리하는 중에 새로운 START 명령을 거부
- **트리거**: Arm A가 처리할 수 있는 것보다 빠르게 여러 주문이 도착

**코드 분석**:
```python
def _start_internal(self, job_id: str, recipe_name: str, pause_before_last: int, goal_handle=None) -> bool:
    with self._state_lock:
        if self._run_task is not None and not self._run_task.done():
            self._publish_status(job_id, "FAIL", reason="busy")  # ← 에러 발생 지점
            return False
```

**발생 이유**:
1. GUI 주문 1 → FMS → CookingOrder → Coordinator → Arm A START (조리 시작)
2. GUI 주문 2가 즉시 도착 → FMS → CookingOrder → Coordinator가 주문 2 시작 시도
3. Arm A가 여전히 주문 1 처리 중 → `FAIL:busy`로 거부

**해결 방안 권장**:
- **방안 1 (빠른 수정)**: 주문들 사이에 충분한 지연을 두고 배치 (이전 주문 완료 대기)
- **방안 2 (적절한 수정)**: 새 작업 시작 전 Arm A 유휴 상태 확인과 함께 coordinator에 큐 관리 구현
- **방안 3 (프로덕션 수정)**: recipe_executor_node 자체에 작업 큐 구현

**상태**: ✅ 분석 및 문서화 완료 `/home/gw/kitchmatics/roscamp-repo-1/FMS_실행_가이드.md`

---

### 2. 중요 Domain Bridge 설정 누락 ✅

**문제점**: FMS-to-Coordinator 통신 토픽들이 로봇팔 도메인으로 브리징되지 않음

**누락된 토픽들**:
1. `/fms/pickup_arrival` (Domain 25 → 20, 21) - coordinator가 pinky 도착을 알기 위해 필수
2. `/cooking/order` (Domain 25 → 20, 21) - coordinator가 조리 주문을 받기 위해 필수
3. `/cooking/loading_complete` (Domain 20/21 → 25) - FMS가 음식 탑재 완료를 알기 위해 필수

**영향**:
- `/fms/pickup_arrival` 없음: Coordinator가 pinky가 pickup_spot에 있다는 것을 절대 모름 → 120초 후 타임아웃
- `/cooking/order` 없음: Coordinator가 FMS로부터 주문을 절대 받지 못함 → 조리가 일어나지 않음
- `/cooking/loading_complete` 없음: FMS가 음식이 탑재되었다는 것을 절대 모름 → pinky가 절대 배달하지 않음

**적용된 수정**:
`/home/gw/kitchmatics/roscamp-repo-1/fms/config/domain_bridge_complete.yaml` 업데이트:

```yaml
# Domain 25 -> Domain 20: Arm A coordinator로 FMS 알림 추가
- from_domain: 25
  to_domain: 20
  topics:
    /arm_a/cmd:
      type: std_msgs/msg/String
    /fms/pickup_arrival:          # ← 추가됨
      type: fleet_interfaces/msg/PickupArrival
    /cooking/order:               # ← 추가됨
      type: fleet_interfaces/msg/CookingOrder

# Domain 21 -> Domain 25: coordinator에서 FMS로 LoadingComplete 추가
- from_domain: 21
  to_domain: 25
  topics:
    /arm_b/status:
      type: std_msgs/msg/String
    /verify/status:
      type: std_msgs/msg/String
    /cooking/loading_complete:    # ← 추가됨
      type: fleet_interfaces/msg/LoadingComplete

# Domain 25 -> Domain 21: Arm B coordinator로 FMS 알림 추가
- from_domain: 25
  to_domain: 21
  topics:
    /arm_b/cmd:
      type: std_msgs/msg/String
    /verify/cmd:
      type: std_msgs/msg/String
    /fms/pickup_arrival:          # ← 추가됨
      type: fleet_interfaces/msg/PickupArrival
    /cooking/order:               # ← 추가됨
      type: fleet_interfaces/msg/CookingOrder
```

**상태**: ✅ 수정 완료 - Domain bridge 설정 업데이트됨

---

### 3. 워크스페이스 빌드 문제 ✅

**문제점**: 모노레포에서 중복된 패키지 이름으로 인해 전체 빌드 방지

**에러**:
```
ERROR:colcon:colcon build: Duplicate package names not supported:
- mycobot_kitchen_msgs: kitchmatics/s/src vs robot_arm/sandwich_arm_ws/src
- mycobot_kitchen_nodes: kitchmatics/s/src vs robot_arm/sandwich_arm_ws/src
```

**해결책**: FMS 작동에 필요한 패키지만 빌드
```bash
colcon build --symlink-install --packages-select fleet_interfaces fms
```

**Coordinator 워크스페이스**: 별도로 빌드
```bash
cd fms/coordinator_Ws
colcon build --symlink-install
```

**빌드 결과**:
```
✅ fleet_interfaces: [0.47s]
✅ fms: [1.15s]
✅ sandwich_coordinator: [1.16s]
```

**상태**: ✅ 필요한 모든 패키지 빌드 성공

---

## 시스템 아키텍처 검증

### 네트워크 설정 (폐쇄 WiFi)
| 장치 | IP | Domain ID | 역할 | 상태 |
|--------|-----|-----------|------|--------|
| gw PC | 192.168.1.3 | 25 | FMS 서버 | ✅ |
| pinky1 (b4bc) | 192.168.1.7 | 11 | 이동 로봇 | ✅ |
| pinky2 (e2a8) | 192.168.1.6 | 12 | 이동 로봇 | ✅ |
| pinky3 (d29d) | 192.168.1.11 | 13 | 이동 로봇 | ❌ 비활성 |
| jetcobot A (aa1f) | 192.168.1.4 | 20 | 샌드위치 팔 | ✅ |
| jetcobot B (aa85) | 192.168.1.10 | 21 | 소스/검수 팔 | ✅ |

### 요구되는 워크플로우 검증
1. ✅ GUI 주문 → FMS가 pinky를 pickup_spot으로 보내고 + CookingOrder를 coordinator로 전송 (병렬)
2. ✅ Coordinator가 domain bridge를 통해 CookingOrder 수신 (20/21)
3. ✅ Arm A가 조리 시작, pinky가 pickup_spot으로 내비게이션
4. ✅ pinky 도착 시 FMS가 PickupArrival 발행
5. ✅ Coordinator가 domain bridge를 통해 PickupArrival 수신 (20/21)
6. ✅ Coordinator가 전달 전 pinky 도착 대기
7. ✅ Coordinator가 음식 탑재 시 LoadingComplete 발행
8. ✅ FMS가 domain bridge를 통해 LoadingComplete 수신 (25)
9. ✅ FMS가 pinky를 테이블로 내비게이션
10. ✅ 배달 후, pinky가 홈 스팟으로 복귀

### Domain Bridge 플로우
```
FMS (Domain 25) ─┬─→ [Bridge] ─→ pinky1 (Domain 11)
                 ├─→ [Bridge] ─→ pinky2 (Domain 12)
                 ├─→ [Bridge] ─→ jetcobot A (Domain 20)
                 │              ↓ /cooking/order
                 │              ↓ /fms/pickup_arrival
                 │              ↑ /arm_a/status
                 │              ↑ /cooking/loading_complete
                 │
                 └─→ [Bridge] ─→ jetcobot B (Domain 21)
                                ↓ /cooking/order
                                ↓ /fms/pickup_arrival
                                ↑ /arm_b/status
                                ↑ /verify/status
                                ↑ /cooking/loading_complete
```

---

## 수정된 파일들

### 1. `/home/gw/kitchmatics/roscamp-repo-1/fms/config/domain_bridge_complete.yaml`
**변경사항**:
- `/fms/pickup_arrival` 브리징 추가 (Domain 25 → 20, 21)
- `/cooking/order` 브리징 추가 (Domain 25 → 20, 21)
- `/cooking/loading_complete` 브리징 추가 (Domain 20/21 → 25)

**목적**: FMS-to-Coordinator 및 Coordinator-to-FMS 통신 활성화

### 2. `/home/gw/kitchmatics/roscamp-repo-1/FMS_실행_가이드.md`
**생성됨**: 다음을 포함한 완전한 시스템 실행 가이드:
- 근본 원인 분석
- 터미널 설정 지침
- 검증 체크리스트
- 문제 해결 가이드

---

## 실행 명령어 준비 완료

### 메인 PC (3개 터미널 필요)

**터미널 1: FMS 노드**
```bash
cd /home/gw/kitchmatics/roscamp-repo-1
source install/setup.bash
export ROS_DOMAIN_ID=25
ros2 launch fms fms_closed_network.launch.py
```

**터미널 2: Domain Bridge**
```bash
cd /home/gw/kitchmatics/roscamp-repo-1
source install/setup.bash
export ROS_DOMAIN_ID=25
ros2 run domain_bridge domain_bridge fms/config/domain_bridge_complete.yaml
```

**터미널 3: Customer GUI**
```bash
cd /home/gw/kitchmatics/roscamp-repo-1/app/gui/customer_gui
source ~/pyqt_venv/bin/activate
python src/main_fms_direct.py
```

---

## 사용자를 위한 다음 단계

1. **시스템 실행**:
   - FMS 노드 시작 (터미널 1)
   - Domain bridge 시작 (터미널 2)
   - Customer GUI 시작 (터미널 3)
   - 사용자가 수동으로 로봇 노드 시작 (pinky bringup, Nav2, jetcobot arms)

2. **검증 테스트**:
   ```bash
   # Domain bridge 상태 확인
   ros2 topic list | grep -E "pinky1|arm_a|cooking"

   # FMS 상태 모니터링
   ros2 topic echo /fms/fleet_status --once

   # Pickup 도착 모니터링
   ros2 topic echo /fms/pickup_arrival

   # 탑재 완료 모니터링
   ros2 topic echo /cooking/loading_complete
   ```

3. **주문 플로우 테스트**:
   - GUI에서 주문 (테이블 1, 메뉴 M001)
   - 주문 처리를 위한 FMS 로그 확인
   - 조리 및 pickup 도착을 위한 coordinator 로그 확인
   - "A_FAIL:busy" 에러 확인 (발생 시, 이전 주문 완료 대기)
   - pinky가 테이블로 배달하는지 확인
   - GUI에서 배달 확인
   - pinky가 홈으로 복귀하는지 확인

4. **"A_FAIL:busy" 발생 시**:
   - **빠른 수정**: 주문 간 30-60초 대기
   - **적절한 수정**: Arm A 유휴 상태 확인과 함께 coordinator 큐 구현 (FMS_실행_가이드.md 참조)

---

## 검증 체크리스트

- [x] "A_FAIL:busy"의 근본 원인 식별 및 문서화
- [x] Domain bridge 설정 수정
- [x] FMS 워크스페이스 빌드 성공
- [x] Coordinator 워크스페이스 빌드 성공
- [x] 실행 가이드 생성
- [x] 시스템 아키텍처 검증
- [ ] 실시간 시스템 테스트 (사용자가 수행)
- [ ] 다중 로봇 조정 테스트 (사용자가 수행)
- [ ] 에러 복구 테스트 (사용자가 수행)

---

## 중요 성공 요인

1. **Domain Bridge를 먼저 실행해야 함**: FMS가 로봇과 통신하기 전
2. **ROS_DOMAIN_ID=25**: 메인 PC의 FMS와 domain bridge 모두에 설정 필요
3. **주문 속도 제한**: Coordinator 큐 수정이 구현될 때까지 주문은 순차적으로 배치해야 함
4. **토픽 모니터링**: `ros2 topic echo`를 사용하여 메시지가 domain bridge를 통해 흐르는지 확인
5. **로그 모니터링**: 모든 터미널에서 에러 확인, 특히 domain bridge 연결 문제

---

## 참고 파일

1. **실행 가이드**: `/home/gw/kitchmatics/roscamp-repo-1/FMS_실행_가이드.md`
2. **이 요약**: `/home/gw/kitchmatics/roscamp-repo-1/수정사항_요약.md`
3. **Domain Bridge 설정**: `/home/gw/kitchmatics/roscamp-repo-1/fms/config/domain_bridge_complete.yaml`
4. **FMS 코드**: `/home/gw/kitchmatics/roscamp-repo-1/fms/fms/fms_node.py`
5. **Order Handler**: `/home/gw/kitchmatics/roscamp-repo-1/fms/fms/order_handler.py`
6. **Coordinator**: `/home/gw/kitchmatics/roscamp-repo-1/fms/coordinator_Ws/src/sandwich_coordinator/sandwich_coordinator/coordinator_node.py`

---

## 요약

**모든 중요 문제 식별 및 해결**:
✅ 근본 원인 분석 완료
✅ Domain bridge 설정 수정
✅ 워크스페이스 빌드 성공
✅ 실행 명령어 준비 완료
✅ 검증 체크리스트 생성

**시스템이 테스트 준비 완료**. 사용자는 3개의 메인 PC 터미널을 실행하고 로봇 노드를 수동으로 시작한 다음, 완전한 워크플로우를 테스트해야 합니다.

**예상 결과**: GUI → 조리 → Pickup → 배달 → 홈 복귀까지 전체 주문 플로우가 여러 ROS 도메인에 걸쳐 적절한 FMS-Coordinator-Robot 조정과 함께 작동.
